# Implementation Decisions

| Date | Decision | Why |
|------|----------|-----|
| 2026-06-28 | Single binary, multiple subcommands | Simpler distribution — one install, one PATH entry |
| 2026-06-28 | JSON to stdout only, errors to stderr | Claude reads stdout; mixing output formats breaks parsing |
| 2026-06-28 | Optional dependency with graceful fallback | Foreman must work without it; foreman-tools is an optimization, not a requirement |
| 2026-06-28 | Zig 0.16, macOS only for v1 | Zig pre-1.0 stability; macOS is the only current target platform |
| 2026-06-28 | Distributed via homebrew-foreman tap | No new tap; installs alongside foreman-ai in one step |
| 2026-06-28 | status reads already-fetched refs, does not git fetch | Latency: fetch adds 200-500ms on every session open; ref reads are instant |
| 2026-06-28 | Added M4 gh-user, M5 release-info, M6 repo-info to spec | Audit of 14 command/skill files found these patterns repeated across 3–5 files each; one JSON read replaces 2–3 shell calls + Claude parsing |
| 2026-06-28 | Added M7 tag-exists to spec | foreman-tools audit found `git tag \| grep "^v<version>$"` in both `/release` and `/brew-release` Step 2 pre-flight — candidate promoted per repetition rule |
| 2026-06-28 | comptime StaticStringMap for FRAMEWORK_MAP, CONFIG_FILE_MAP, SKIP_DIR_SET (v0.9.0) | O(1) lookups at zero runtime cost; replaces O(n) linear scans that ran on every file entry during scan |
| 2026-06-29 | grep subcommand uses literal string search, not regex (v0.10.0) | Covers the dominant Claude usage pattern (find a symbol/string); avoids pulling in a regex engine; can upgrade to regex in a future version |
| 2026-06-29 | grep caps at 500 matches and skips files >5 MB (v0.10.0) | Prevents unbounded JSON output that would consume the token budget the subcommand is meant to save |
| 2026-06-29 | parse-stack reads from stdin, not a file arg (v0.10.0) | Stack traces arrive inline in Claude's context, not as files on disk; stdin lets the caller pipe directly without writing a temp file |
| 2026-06-29 | find-files glob supports only *.ext / prefix* / *contains* / exact / * (v0.11.0) | Covers every real-world pattern without pulling in a regex engine; ** recursive glob deferred until a concrete need surfaces |
| 2026-06-29 | find-files caps at 2000 results (v0.11.0) | Beyond 2000 matches the JSON output itself becomes a token problem; caller should narrow the glob instead |
| 2026-06-29 | json-query value field is a raw JSON fragment, not a re-encoded string (v0.12.0) | Avoids double-encoding; caller reads the value directly without a second parse step |
| 2026-06-29 | json-query uses dot notation with numeric segments for array indexing (v0.12.0) | Keeps the path syntax uniform; `items.1` is simpler to pass as a CLI arg than `items[1]` |
| 2026-06-29 | git-diff runs two git calls (--numstat + --name-status) and correlates them (v0.13.0) | --numstat gives clean addition/deletion counts; --name-status gives A/M/D/R status; neither alone gives both |
| 2026-06-29 | list-dir sorts dirs first then files alphabetically (v0.13.0) | Matches mental model of ls -la; dirs up front means structure is visible before file noise |
| 2026-06-29 | env-scan returns keys only, never values (v0.14.0) | Values are secrets; keys are all Claude needs to understand what a project requires |
| 2026-06-29 | toml-query reuses JsonQueryResult (v0.14.0) | Same shape as json-query means callers handle both identically; no new struct needed |
| 2026-06-29 | toml-query TOML parser is line-by-line, no full AST (v0.14.0) | Covers 95% of real usage (Cargo.toml, pyproject.toml) without a full TOML library; handles [[array-of-tables]] by ignoring it |
| 2026-06-29 | tarball-sha sleep uses std.posix.system.nanosleep (v0.16.0) | std.time.sleep and std.posix.nanosleep don't exist in Zig 0.16; direct POSIX syscall via posix.system is the correct path |
| 2026-06-29 | formula-info parser is line-by-line, not a Ruby AST (v0.17.0) | Covers all real Homebrew formulas (url/sha256/version are always plain quoted fields); avoids pulling in a Ruby parser |
| 2026-06-29 | validate-hooks returns false on missing/malformed file, not an error (v0.18.0) | Callers (/setup-automation, /first-run) already handle the false case; erroring adds no new information and complicates caller logic |
| 2026-06-29 | validate-hooks uses std.c.getenv for HOME (v0.18.0) | std.posix.getenv doesn't exist in Zig 0.16; std.c.getenv is the correct path for libc-backed targets |
| 2026-06-29 | gh-release passes --notes-file to gh, not --notes (v0.19.0) | --notes requires shell escaping of newlines/quotes/backticks; --notes-file lets gh read content directly, eliminating the escaping problem entirely |
| 2026-06-29 | api-schema.md locks all 24 subcommand output shapes | Schema drift is the highest-cost failure mode for downstream modules (Context Builder, Cache Engine); a single source of truth forces a version bump before any shape change |
| 2026-06-29 | file-hash computes SHA256 of a local file (v0.20.0) | Reuses existing sha256Hex helper from tarball-sha; foundation for cache-engine M2 change detection; pure read, no persistent state yet |
| 2026-06-29 | cache-check persists hash to ~/.cache/foreman-tools/ (v0.21.0) | First write operation in foreman-tools; uses std.Io.Dir.createDirAbsolute(.default_dir) + createFileAbsolute; write failures silently ignored so the result is always correct even if caching fails; one file per tracked path keyed by SHA256(file_path) |
| 2026-06-29 | cache-store/cache-fetch keyed by SHA256(file_path + ":" + sub_key) (v0.22.0) | Cache entry format: "<sha256>\n<value>"; auto-invalidates on file change; std.mem.trimEnd (not trimRight) for trailing newline strip; value capped at 512KB |
| 2026-06-29 | context-scan builds on computeScan, distills to top-10 + kind counts (v0.23.0) | Avoids duplicating scan logic; calls computeScan internally and frees the full result after extracting the compact summary |
| 2026-06-29 | KindCounts.test field must be declared and accessed as @"test" in Zig 0.16 | `test` is a reserved keyword; `test: u32` in a struct body and `counts.test` in expressions both fail to parse — must use `@"test": u32` and `counts.@"test"` throughout |
| 2026-06-29 | context-rank reads first 8KB per file for relevance scoring (v0.26.0) | 8KB covers most small-medium files fully; large files (root.zig at 118KB) are partially scored — name match bonus (300 pts) compensates when the file name is meaningful; score = hits×5 + nameMatch×300 + kind_bonus |
| 2026-06-29 | scan now excludes .DS_Store + binary extensions via shouldSkipScanFile (v0.26.0) | SKIP_FILE_SET (.DS_Store, Thumbs.db) + BINARY_EXT_SET check applied in walkScanFiles — affects scan, context-scan, context-rank, find-files |
| 2026-06-29 | context-changed reuses computeGitDiff's numstat+name-status pattern, adds per-file unified diff (v0.25.0) | three git calls per invocation: numstat, name-status, then one `diff --unified=3 -- <path>` per file; diff capped at 100 lines; std.ArrayList(.empty) works in Zig 0.16 (only .init(gpa) fails) |
| 2026-06-29 | context-evidence uses two-pass slice allocation, not std.ArrayList (v0.24.0) | std.ArrayList([]const u8).init(gpa) fails in Zig 0.16 — the type resolves to ArrayListAligned which has no .init; avoid ArrayList entirely; count first, allocate exact size, fill in second pass |
| 2026-06-29 | context-evidence window merging: allocate match_count slots for windows, merge in place, cap at EVIDENCE_MAX_CHUNKS | worst case = match_count non-overlapping windows; over-allocate and track merged_count; no dynamic growth needed |
| 2026-06-29 | yaml-query uses indentation-based block-style parser, not a full YAML AST (v0.27.0) | covers 95%+ of real-world YAML (GitHub Actions, docker-compose, k8s); avoids pulling in a YAML library; trimLeft→trimStart, trimRight→trimEnd in Zig 0.16 |
| 2026-06-30 | outline uses line-by-line pattern matching, not a full AST (v0.28.0) | covers 12 languages; extracts top-level and nested definitions (no indentation filtering — trimmed lines look top-level); capped at 200 symbols; skips lines starting with comment chars before language dispatch |
| 2026-06-30 | deps errdefer uses `for (...) { ... } gpa.free(buf)` — no `;` between for-block and next statement (v0.29.0) | Zig 0.16: for-block is a compound statement, trailing `;` after `}` is a parse error |
| 2026-06-30 | atomic writes via `atomicRenameAbsolute` helper + `std.c.rename` (v0.30.0) | `writeCacheEntry` and `computeCacheStore` both wrote directly via `createFileAbsolute` — power loss mid-write left a corrupted cache entry; fix: write to `{entry_path}.tmp`, flush, close, then `std.c.rename(tmp, final)`; `std.c.rename` is the correct path in Zig 0.16 (libc-backed); `std.posix.rename` does not exist in 0.16 |
| 2026-06-30 | `device-scan` subcommand spec locked (M30, v0.30.0) | Profile hardware+tools+optimal at first install, store to `~/.foreman/profile.json`; Claude reads this at session start instead of re-discovering the environment; community contribution to `foreman-env` repo is opt-in with explicit consent and strips all user paths before sharing |
| 2026-06-30 | `run-tests` implemented (M28, v0.31.0) | Framework detection via manifest files (package.json→jest/vitest, pytest.ini/conftest.py/pyproject.toml→pytest, go.mod→go, Cargo.toml→cargo, build.zig→zig); uses `env -C <path>` as cwd mechanism (macOS 12+, no `.cwd` field in std.process.run in Zig 0.16); timing via `std.c.clock_gettime(CLOCK.MONOTONIC)` (std.time.nanoTimestamp does not exist in Zig 0.16); per-framework text parsers (zig/cargo/go/pytest/jest); failures capped at 50; `@"test"` field name required since `test` is a Zig keyword |
| 2026-06-30 | `build` implemented (M29, v0.32.0) | Detection order: Cargo.toml→cargo, build.zig→zig, go.mod→go, package.json(with "scripts"+"build")→npm, Makefile→make; cargo parser is a state machine (error[Exx]: msg + " --> file:N:M" two-line pair); zig/gcc/clang parser uses lastIndexOf(":") twice to split path:N:M from ": error: " marker; go parser matches ".go:" prefix pattern; TypeScript parser matches "file(N,M): error" paren-coord format; errors capped at 50, warnings at 20; `truncated` is a single shared bool across both arrays |
| 2026-06-30 | `env-inspect` implemented (M4, v0.33.0) | Manifest-gated language detection (go.mod→go, requirements.txt/pyproject.toml→python, package.json→node, Cargo.toml→rust, build.zig→zig, Gemfile→ruby, pom.xml/build.gradle→java); `checkBinary(bin, flag)` runs binary + collects stdout+stderr; `extractVersionStr` finds first N.N[.N] pattern (handles v-prefix, embedded versions like go1.22.0); package managers always checked (npm/pip/cargo/brew/yarn/pnpm); missing items include runtime gaps and uninstalled deps (node_modules, .venv, vendor/bundle); envVars from .env* files via existing parseEnvKeys |
| 2026-06-30 | `metrics` implemented (M26, v0.48.0) | Walks `~/.cache/foreman-tools/` with `std.Io.Dir.openDirAbsolute(io, path, .{ .iterate = true })` + `it.next(io)` to count cache entries; walks `~/.foreman/state/` to count .json files and sum decisions+known_patterns per file via `std.json.parseFromSlice`; uses `std.c.getenv("HOME")` + `std.mem.span` for HOME; all dir-walks wrapped in labeled blk with `catch break :blk` (missing dirs → zeros); `cache_size_bytes` dropped (no `statFile` in Zig 0.16 Io.Dir); `MetricsResult` has no owned fields — no gpa.free needed in dispatch; `estimatedTokenSavings = cache_entries *% 160` (wrapping multiply for overflow safety); `device_profiled` and `compat_baseline_set` use `fileExists(io, path)` helper |
| 2026-06-30 | `report` implemented (M23, v0.47.0) | Calls computeGitCache, computeQualityGate, computeSecretScan each in labeled blk with bare `catch { break :blk null }`; issues ArrayList(ReportIssue) built from critical/high/medium findings; secret count converted to allocPrint message (owned → must match free pattern); status derived after all sub-calls: has_critical → "blocked", has_high or issues.len > 0 → "issues", else "clean"; nextAction is a string literal (not owned); git_branch is duped only when non-empty (empty string literal otherwise — not freed); confidence driven by which sub-calls succeeded |
| 2026-06-30 | `route` implemented (M3, v0.46.0) | Calls `computeCapabilityCheck` then looks up matching subcommand in `ROUTE_ENRICHMENTS` static table (25 entries with context-aware arg hints + reasons); `name_match_count`/`desc_match_count` hoisted outside the else block so they're in scope for tie-breaker; `gpa.alloc(RouteStep, 0)` for the no-match case (never a comptime slice that can't be freed); steps[].string fields borrow from static literals; `result.task` and `result.steps` slice both freed by main.zig dispatch; scoring fix: multi-word desc matches (≥2 → 45) now beat single-word name matches (35) |
| 2026-06-30 | `capability-check` implemented (M2, v0.45.0) | Reuses `computeRegistry()` static slice; lowercase query + per-subcommand lowercase via per-field alloc+defer; `tokenizeScalar(u8, q_lower, ' ')` skips empty tokens; words < 3 chars filtered (stop-word elimination); scoring: exact name=100, name-contains/contains-name=80, all-words-in-name=70, all-in-desc=50, any-in-name=40, any-in-desc=30, threshold=30; result.query is duped (caller must free via `defer gpa.free(result.query)`); subcommand/description/args fields borrow from static registry (string literals, not duped); available=false → null JSON for optional fields |
| 2026-06-30 | `registry` implemented (M1 partial, v0.44.0) | Pure comptime static data — `computeRegistry()` returns a `RegistryResult` with `VERSION` and a `[]const RegistrySubcommand` literal; zero allocations, no IO; dispatch in main.zig iterates the slice and prints JSON array; self-referential (registry lists itself as last entry); foundation for Module 1 Foreman Core capability discovery |
| 2026-06-30 | `prod-ready` implemented (M24-M1-M5, v0.43.0) | Calls `computeQualityGate` (skips on error → warning), `computeSecretScan` (findings → blocker), `computeEnvInspect` (missing → warnings, bare `catch` for errors); quality-gate critical/high → blockers with build_errors/test_fails counted from `f.source` field; fault-tolerant: each check wrapped in labeled blk with `catch { break :blk null }`; `_ = e` pattern caused compiler error → use bare `catch` instead; GPA leak warnings go to stderr, not stdout — JSON output clean |
| 2026-06-30 | `validate-schema` implemented (M16-M5, v0.42.0) | JSON Schema subset validator; `validateValue(data, schema, path, depth)` recursive to depth 6; `jsonMatchesType` maps JSON variants to schema type names (integer matches "integer" and "number", float only matches "number"); labeled blocks (`break :min_len`) used instead of goto for constraint skipping; violations store `[]const u8` slices — required-field paths are direct allocPrint (no defer), property-descend paths use defer-freed temporaries with the actual violation storing a duped copy; `additionalProperties: false` flags unexpected keys |
| 2026-06-30 | `quality-gate` implemented (M15-M1-M3, v0.41.0) | Calls `computeBuild` (skips on `error.NoBuildSystem`) and `computeRunTests` (skips on `error.NoTestFramework`); build errors → `high`, build crash (success:false + errors.len==0) → `critical` with allocPrint message, build warnings → `medium`; test failures → `high`, test runner crash (success:false + failures.len==0 + failed==0) → `critical`; verdict = "fail" if critical.len>0 or high.len>0, else "pass"; findings reuse pointers from BuildResult/RunTestsResult (no dupe — process exits immediately); capped at 50 per level |
| 2026-06-30 | `shell-run` implemented (M9-M1-M4, v0.40.0) | Runs `/bin/sh -c <cmd>`; destructive check via `shellRunBlockReason` on lowercased first 4KB of command (uses `std.ascii.lowerString` into stack buf); `shellRunRmRfDanger` checks that char after `rm -rf /` is space/special/end (not a letter = specific path); exit code from `.term.exited \|c\|` (lowercase); duration from MONOTONIC clock diff in ms; `timedOut = duration_ms >= timeout_ms` (retrospective, not enforced); stdout/stderr owned by gpa, display-capped at 128KB; blockReason is a string literal (not owned) |
| 2026-06-30 | `project-state` implemented (M28-M1-M2, v0.39.0) | State file at `~/.foreman/state/ps-{sha256(abs_path)}.json`; `computeProjectState` accepts `ProjectStateMode` union (read / record_decision / record_pattern); `parseProjectStateFile` reads+parses via `std.json.parseFromSlice(std.json.Value,...)`; `writeProjectStateFile` uses `w.interface.writeAll`/`print`/`flush` + `atomicRenameAbsolute`; `allocJsonEscape` (already in root.zig at line ~407) handles user-supplied text; date from `std.c.clock_gettime(CLOCK.REALTIME)` → `tsToDateStr` (manual Gregorian calculation); capped at 100 decisions / 50 patterns |
| 2026-06-30 | `git-cache` implemented (M14-M3, v0.38.0) | Cache keyed by HEAD SHA (not file hash): cache file `gc-{sha256(repo)}.json`, first line is stored HEAD SHA, body is JSON state; `std.json.parseFromSlice` parses cache on hit; miss path calls `runGit` for branch/status/ahead-behind/log; `--left-right --count HEAD...@{u}` gives ahead\tbehind in one call; cache invalidates automatically on any commit |
| 2026-06-30 | `delta-context` implemented (M13-M2, v0.37.0) | `git diff --name-only [ref]` → parse `@@ -old +new,count @@` hunk headers → `computeOutline` per file → `findOwningSymbol` maps changed lines to symbol bodies (symbol owns from its line until next symbol's line) → `computeSymbolFind` per symbol for callers; capped at 8 files / 10 symbols / 10 callers; lone `}` in Zig format string needs `}}` escape |
| 2026-06-30 | `device-scan` implemented (M30-M1, v0.36.0) | `sysctl -n machdep.cpu.brand_string/hw.physicalcpu/hw.memsize` + `uname -m` for hardware; `checkBinary` (reused from env-inspect) for 7 tools; `deviceScanZigFlags` maps M1–M5 chip names to `-Dcpu=apple_mN`; profile written atomically to `~/.foreman/profile.json`; `std.fmt.bufPrint` into stack buffer for tools JSON (std.ArrayList.init/std.io.fixedBufferStream don't exist in Zig 0.16); `computeCacheStore` called inline to pre-warm cache so next `cache-fetch` hits immediately |
| 2026-06-30 | `secret-scan` implemented (M19-M1, v0.35.0) | Two detection modes: prefix-match (high-confidence token formats like sk_live_, AKIA, ghp_, PEM headers) and assignment-key (case-insensitive needle in variable name + non-placeholder value); `secretScanIsCleanKey` rejects compound-statement keys (containing `(`, `)`, `{`, `}`, `;`); `secretScanIsPlaceholder` rejects digit/dot-starting values, type declarations (struct/enum/union), env var references, and known placeholder strings; skips comment lines + binary files + .example files |
| 2026-06-30 | `symbol-find` implemented (M6-M2, v0.34.0) | Single-pass walk with keyword-based declaration detection (fn/def/function/func/class/struct/trait/const/var/let/val + 10 more); whole-word boundary matching via `isWordChar`; magic-byte binary skip (Mach-O 3 variants + ELF); definition = first keyword match, references = remaining whole-word hits; capped at 100 refs; replaces grep+read-N-files pattern |
| 2026-06-30 | `compat-check` implemented (M31, v0.30.0) | Pure Zig, ~20ms, zero Claude tokens; `parseBaselineField` returns slice into content (not owned) for safe in-place parsing; `extractVersionFromLine` strips leading v/V and finds first digit-starting token — handles zig/git/gh/brew/node/python3 output formats without special cases; rollback commands include exact version specifier from baseline (e.g. zig@0.15); risk levels: high=zig+foreman_tools, medium=node+python3, low=git+gh+brew; baseline stored atomically to ~/.foreman/compat-baseline.json |
| 2026-07-01 | `allocJsonEscape` two-pass fast path (v0.56.0) | Pre-scan counts extra bytes; if zero, `gpa.dupe(u8,s)` — no allocation of a new buffer, no per-char branch overhead in the common all-ASCII case; eliminates ArrayList overhead at 57+ callsites |
| 2026-07-01 | `serializeJsonValue` pre-sized ArrayList (v0.56.0) | `ensureTotalCapacity(2 + items.len*32)` for arrays, `*(48)` for objects; avoids growth reallocations during recursive JSON serialization in state-merge |
| 2026-07-01 | `hasBinaryMagic` uses `readSliceShort` (v0.56.0) | Reads first 4 bytes into a stack `[8]u8` read buffer; `allocRemaining(.limited(N))` errors with `StreamTooLong` if file has MORE than N bytes — wrong API; `readSliceShort` reads exactly the requested count or fewer without consuming the rest |
| 2026-07-01 | `zig-cache` added to `SKIP_DIR_SET` (v0.56.0) | Zig build creates `zig-cache/` (no dot prefix) alongside `.zig-cache/`; both must be excluded or build artifacts rank above source files in all scan-based subcommands |
| 2026-07-01 | Pre-built binaries removed from repo (v0.56.0) | Distribution via Homebrew formula only; `foreman-tools`, `foreman-tools-arm64`, `foreman-tools-x86_64` git-rm'd and added to `.gitignore`; Homebrew builds from source tarball |
| 2026-07-01 | `ledger` subcommand + `ledger score` implemented (v0.57.0) | Storage: `~/.foreman/ledger.json`; five modes: show, record, check-stale, validate, score; score computes composite from cited live sources (10pt each, -10 if contradicted), requires ≥10 sources and 100% composite for a win; Zig tiebreaker: valid stored entry beats 100% Claude reasoning; staleness: 365 days via `revalidation_due_ts`; question matching: case-insensitive substring both directions (conservative — Zig wins more than it should, never less); ID: first 16 hex chars of sha256(winner:question:date) — 2^64 values sufficient for personal ledger; append-only, old entries superseded not deleted |
| 2026-07-01 | `tui` subcommand implemented (v0.58.0) | TUI uses Zig→Python worker pattern: Zig gathers project data as JSON via `computeListProjects`, spawns Python3 via `execv` with an embedded curses script (`@embedFile("tui.py")`); 24-char left panel lists projects, right panel shows release state + MVP readiness checklist; j/k/arrow nav, r reload, q quit; SIGWINCH resize; avoids complex Zig termios/ioctl — Python curses is the right layer for interactive rendering |
| 2026-07-01 | `knowledge-audit` subcommand implemented (v0.59.0) | 7-check gate: spec.md present, CLAUDE.md has decision log rows, knowledge/ files exist, _knowledgebase/ has mirror entry, git repo is clean, HEAD is pushed, ledger refs the project; `ready: true` only when `unextracted` and `warnings` are both empty; designed as the pre-export gate — must pass before a project can be safely archived or deleted; file reads via `kaReadFile` helper; decision log rows counted by "| 20" date prefix pattern |
| 2026-07-01 | `export` + `import` subcommands implemented (v0.60.0) | Export formats: fmz (tar.gz containing foreman.manifest.json + git-archive of project source + knowledge/), brew/mac/linux/windows (installer scripts), backup (full workspace snapshot including all project .fmz files + ledger + framework files); Import: detects .fmz vs raw directory, reads manifest to distinguish workspace backup from single project, copies project/ tree to foreman_root/<name>, carries over knowledge/; atomic temp-dir staging via /tmp/foreman-export-<pid>; never calls rm — directories are left in /tmp for OS cleanup |
| 2026-07-02 | `context-budget` subcommand implemented (M35, unreleased) | Reuses `computeFileStats` per path (no new file-read primitive); `FileNotFound` on any path degrades to a zero-byte entry rather than failing the whole call, so a partially-stale file list still returns a usable estimate; risk thresholds hardcoded at <2000 tokens low, <8000 medium, else high — no config flag yet |
| 2026-07-02 | `context-gate` subcommand implemented (M34, unreleased) | Orchestrator only — no new scanning/ranking logic. Calls `computeContextRank` (top files by relevance to the task string), `computeContextChanged` (working-tree diff, `error.GitFailed` treated as "no diff" rather than a hard failure so it works outside git repos), and `computeSecretScan` (redaction gate) and composes their outputs into one manifest; `--test-cmd` / build-error capture from the original spec sketch deferred — `include.errors` always returns `[]` in this cut; token estimate = (sum of included file bytes + diff bytes) / 4, same thresholds as `context-budget`; `send_to_ai` is false whenever secrets are found or risk is high, never both-and — either condition alone blocks it |
| 2026-07-02 | `context-classifier` subcommand implemented (M36, unreleased) | Pure keyword table (`CLASSIFIER_SIGNALS`), no ML; multi-word phrases ("compile error", "extract module") ordered before generic single words ("fix", "add") so specific signals aren't drowned out, but generic words still fire on unrelated text — a known precision ceiling for a keyword-only classifier; confidence is relative (winner / total matches), not absolute — a task with one weak match still reports confidence 1.0; not yet called from `context-gate`, wiring deferred to when Phase 2 is assembled end-to-end |
| 2026-07-02 | `context-dependency-graph` subcommand implemented (M37, unreleased) | Line-based, not AST-based — reuses `outlineDetectLang` for language detection but writes new per-language import-line extractors (go/python/js/ts/rust/zig) since import syntax varies too much for `outline`'s existing symbol-detection helpers to reuse directly; Go block-style imports (`import (\n "fmt"\n)`) only caught for bare unaliased lines — aliased block imports (`f "fmt"`) are a known gap; `importedBy` is a basename substring scan across the whole project (capped at 50 hits, 32KB read per candidate file), explicitly heuristic — a file that mentions the target's name in a comment or string matches too; hit the same `allocRemaining(.limited(N))` → `StreamTooLong`-on-oversized-file bug already documented for `hasBinaryMagic` (2026-07-01 entry) on the very first real-file test (`src/main.zig` is well over the 32KB read cap) — fixed the same way, `readSliceShort` into a heap-allocated destination buffer instead |
| 2026-07-02 | `context-compressor` subcommand implemented (M38, unreleased) | Head/tail truncation (default 200 lines: 100 head + 100 tail) with an `"... N lines omitted ..."` marker — no semantic summarization; files under the cap are returned byte-identical, not re-serialized, so `compressedLines == originalLines` is a reliable "nothing was touched" signal for callers |
| 2026-07-02 | Fixed relative-path crash across 11 subcommands (unreleased) | Root cause: every `*Absolute` API in `std.Io.Dir` is undefined behavior on a relative path — confirmed by reproducing `4orman-tools build .` corrupting allocator state (panic: reached unreachable code, inside `Allocator.free`/`std.debug.assert`) while the identical absolute-path invocation succeeded cleanly. Originally misdiagnosed as a Zig 0.16 `.name = .identifier` zon-syntax parser gap (spec.md row 1) — that theory was wrong, the actual trigger is purely path-shape, reproducible on any project. Added `root.resolveAbsolutePath` (wraps `std.Io.Dir.cwd().realPathFileAlloc`) and wired it into `scan`, `context-scan`, `outline`, `env-inspect`, `secret-scan`, `deps`, `build`, `run-tests`, `quality-gate`, `prod-ready`, `report` — the 11 confirmed-crashing/highest-value dispatch sites. ~34 other path-taking subcommands share the same input shape and were not individually verified — follow-up audit still open. Added Tier 4 to `src/stress.zig` (`runIn`/`smokeIn`/`badIn` helpers, subprocess `cwd` override) so relative-path invocation is now covered by the regression suite — no prior tier ever exercised anything but absolute paths, which is why this shipped undetected |
| 2026-07-02 | `update` subcommand implemented (M39, Field Reports #1-3, unreleased) | Storage at `~/.4orman/field-reports/<project_id>/` — same `~/.4orman/` root as `ledger.json`, deliberately not per-project (`<project-root>/.4orman/`) so it survives outside each project's own git history/version control; `project_id` is just `std.fs.path.basename` of the resolved path, matching the literal directory-name examples in the original spec (`zig-factory`, `4orman`). `state.json` uses the same defensive field-by-field `std.json.Value` extraction as `parseLedgerFile` rather than `parseFromSlice(T, ...)` into a typed struct, for the same forward-compatibility reason (schema can grow a field later without breaking old readers). `attempts.log`/`verification.log` are JSON-lines (one object per line, never rewritten) appended via `File.writePositionalAll` at the current `stat().size` offset — Zig 0.16's `Io.File` has no dedicated append-mode open flag or seek-to-end helper, this is the correct substitute. Scope is deliberately narrow: `computeUpdate` discovers (`computeScan`) and verifies (`computeQualityGate`, reused not rebuilt) and writes a checkpoint — it does not yet route to a repair capability or write `solved.toml`/`blocked.toml` (Field Reports #4, not started). Verified end-to-end: first run creates all three files with correct content, second run appends to attempts.log rather than overwriting (resume-compatible), quality-gate pass → `status: "idle"`, nonexistent path returns a clean error rather than a crash |
| 2026-07-02 | `field-report-solve` / `field-report-block` implemented (M40, Field Reports #4, unreleased) | Reuses `appendFieldReportLog` (renamed in intent, not signature — it was already a generic append-text-at-EOF helper, just previously only used for JSON-lines) for `solved.toml`/`blocked.toml` TOML array-of-tables blocks; reuses `allocJsonEscape` for TOML basic-string escaping rather than writing a second escaper — TOML's `\" \\ \n \r \t \uXXXX` escape set is a compatible subset of JSON's. Both subcommands read their entry as JSON on stdin (matching the `cache-store` convention) rather than 7 positional args for `field-report-block`, which would be unusable at a shell prompt. `field-report-block` reads existing `state.json`, demotes `status` to `"blocked"`, sets `resume_point` to `"review-field-reports"` — every write path funnels through the same `state.json` so it stays the single resumable source of truth rather than blocked.toml being a second place callers have to check. Stress coverage gap, disclosed not silent: `std.process.run`'s `RunOptions` has no field for supplying stdin content in this Zig version, so `stress.zig` Tier 6 only covers the no-stdin-needed usage/error paths; the JSON-body happy path was verified manually (piped JSON against a real project, confirmed correct `solved.toml`/`blocked.toml` content and the `state.json` → `"blocked"` transition) |
| 2026-07-02 | Fixed `resolveAbsolutePath` allocator corruption + 4 pre-existing `std.mem.join` leaks (unreleased) | **Corruption bug, self-caught after merge:** `realPathFileAlloc` returns a sentinel-terminated `[:0]u8` (allocated at `len+1` bytes for the null terminator); `resolveAbsolutePath` returned it directly as a plain `![]u8`, silently coercing away the sentinel type. Every caller's later `gpa.free(result)` then reported the wrong size to the allocator — surfaced live as `error(DebugAllocator): Allocation size 40 bytes does not match free size 39` the first time a path of a length that crossed a size-class boundary was resolved, running the just-installed binary directly (not caught by `zig build test`/`stress`, which happened to only exercise lengths that stayed within the same size class — a reminder that passing tests are not proof of correctness for this class of bug). Fix: dupe `realPathFileAlloc`'s result into a fresh, correctly non-sentinel allocation and free the original — costs one extra alloc+copy+free per call, negligible for a CLI tool, and requires no changes at any of the 13 call sites since the public signature is unchanged. **While re-verifying via the Debug build** (which uses `DebugAllocator` and catches this class of bug where `ReleaseSafe` didn't) also found and fixed 4 pre-existing, unconditional `std.mem.join(...) catch ""` results passed inline as format args and never freed, in `computeRunTests`, `computeBuild`, and twice in `computeQualityGate` (`br`/`tr` uncertainty-candidates branches) — genuine leaks, not corruption, but now hit far more often since `update`/`quality-gate`/`prod-ready`/`report` all call these transitively. Remaining known leaks in `computeQualityGate`/`computeProdReady`/`computeReport` (unrelated call sites, pre-existing, never-freed findings arrays) were left alone — out of scope for this fix, disclosed here rather than silently left undocumented. **Process note:** this is exactly the failure the Patch Integrity Gate's step 4 (targeted test) and the Regression Test Generator rule (row 17) are meant to catch before a merge, not after — the gate was followed for `zig build`/`zig build test`, but the actual installed-binary smoke test that caught this ran only after merging PR #7, not before |
| 2026-07-02 | `review-field-reports` subcommand implemented (M41, Field Reports #6, unreleased) | Reuses `computeListDir` to enumerate `~/.4orman/field-reports/*` rather than writing a second directory-walk; only projects with `state.json.status == "blocked"` are included — `state.json` is the authoritative "still needs help" signal, `blocked.toml` itself is not scanned for a per-entry resolved marker because none exists yet. `parseLastBlockedEntry` is a deliberately minimal line-based parser scoped to exactly the fields `computeFieldReportBlock` writes (same "cover what we produce" philosophy as `toml-query` elsewhere in this file) — not a general TOML reader; it walks the whole file and keeps overwriting the current record on every `[[blocked]]` marker, so the last block in the file naturally wins without needing to track array indices. Sorted newest-first by `state.json.last_updated` as an ROI/priority *proxy* — true ROI/determinism/reuse scoring (what the original spec asked for) needs metadata this schema doesn't capture, disclosed as a known gap rather than fabricating a score. Verified end-to-end: two independently-blocked test projects both returned with correctly parsed fields; non-blocked projects correctly excluded; empty/missing `~/.4orman/field-reports/` returns `{"projectsScanned": 0, "blockers": []}` rather than erroring |
| 2026-07-02 | `solutions-record` / `solutions-list` implemented (M42, Field Reports #7, unreleased) | Deliberately a sibling file (`~/.4orman/solutions.json`, JSON-lines) rather than a new field on `ledger.json`'s `LedgerEntry` — that struct has 5 existing construction/parse/write call sites (`computeLedger`'s `record`/`record_jungian`/`record_outcome`/`validate` branches, plus `findLedgerPrecedent`/`findExactLedgerEntry`'s return literals) backing the framework's Claude-vs-Zig scoring protocol that CLAUDE.md's guardrails depend on; widening it for a Medium-priority feature risked that working system for marginal storage-location purity. Reuses `ledgerWordOverlapScore` (unmodified, same function `findLedgerPrecedent` calls) for dedup — the exact same "what counts as related" definition, not a second one — and reuses `appendFieldReportLog` for the append-at-EOF write (that helper was already generic despite its name, per its 2026-07-02 field-reports entry). `applicableProjectTypes` is stored as a single comma-separated string field, not a nested JSON array, to avoid a second array-parsing path in `parseSolutionsFile` for one field. Verified: dedup correctly rejects a near-duplicate `problemPattern` and returns the original entry's id instead of creating a second one; `ledger show`/`~/.4orman/ledger.json` confirmed completely unaffected by any `solutions-record` call (the file doesn't even exist yet on this machine — no ledger `record` mode has been used, only `show`/`check-stale` reads) |
| 2026-07-02 | Wave 5 Phase 2 wired into `context-gate` (M34 extended); 7 more relative-path crashes found and fixed | `computeContextGate` now classifies the task first (`context-classifier`, M36), always; runs `context-dependency-graph` (M37) on the #1 ranked file only for `architecture_refactor` tasks, adding up to `CONTEXT_GATE_MAX_DEP_FILES=4` deduped `importedBy` files as the blast radius; and runs `context-compressor` (M38) on any included file over `CONTEXT_GATE_COMPRESS_THRESHOLD=8KB`, using the compressed size in the token budget — except the #1 ranked file on `compile_error` tasks, which is never compressed (needs exact line fidelity). `include_files` changed from a fixed-size `[]u8` alloc to a `std.ArrayList` since the dependency-graph expansion makes the final count variable. **While testing this, found `context-gate` itself, plus `context-rank`, `context-evidence`, `context-budget`, `context-dependency-graph`, `context-compressor`, and `context-slice` all still had the exact row-1 relative-path bug** — none of them were in the original 11-site fix (PR #5) because most didn't exist yet at that time, and the one that did (`context-gate`, M34) was simply missed. `context-changed` was checked and confirmed safe — it only shells out to `git -C`, never calls a `*Absolute` API. Fixed all 7 with the same `resolveAbsolutePath` pattern (`context-budget` resolves each of its N path args individually, `context-dependency-graph`'s second `rel-file-path` arg is deliberately left unresolved — it's relative-to-root by design). Added Tier 9 to `stress.zig` covering the full family with relative paths plus field-level assertions that `architecture_refactor`/`compile_error` tasks actually classify differently, not just "didn't crash". `zig build test`: pass. `zig build stress`: 124/124. **Remaining known gap, explicitly not fixed here (out of scope per this session's "pause and focus on Context Translator" directive):** ~25 other path-taking subcommands (`list-dir`, `file-stats`, `symbol-find`, `delta-context`, `git-cache`, `knowledge-audit`, `export`, `import`, `ant`, `project-state`, `diff-dirs`, `grep`, `find-files`, `json-query`, `git-diff`, `env-scan`, `toml-query`, `yaml-query`, `list-projects`, `file-hash`, `cache-check`, `cache-store`, `cache-fetch`, `rollback`, `session-snapshot`) have not been individually audited for this bug class and likely share it — a full sweep is still open follow-up work |
| 2026-07-02 | `context-gate` usage instrumentation added; `metrics` extended (M43, unreleased) | `appendContextGateEvent` logs every `context-gate` call to `~/.4orman/context-gate-events.json` (JSON-lines) — best-effort, `catch return` on every I/O step, never fails the actual `context-gate` call. `computeContextGateUsageStats` (new, private) reads that log and aggregates: task-string repeat detection via a `StringHashMap(u32)` keyed on lowercased task text (repeat count = distinct keys with value ≥2, not total occurrences); file repeat frequency via a second `StringHashMap(u32)` across every call's `files` array, sorted descending, top 10 kept. Had to introduce a named `ContextGateUsageStats` struct — Zig 0.16 rejects coercing a `.{...}` anonymous-struct literal assigned to a `const` first (for the early-return-on-missing-file case) into a function's inline anonymous return type; naming the type once resolved it. Separately, `RepeatedFileCount.path` is `[]u8` (owned, for the final returned array) but `StringHashMap.iterator()`'s `kv.key_ptr.*` is `[]const u8` — introduced a local-only `RankedFileRef` struct with `[]const u8` for the intermediate sort pass, only duping into the owned `[]u8` `RepeatedFileCount` for the final top-10 slice, avoiding a const-discard error. `cacheWired` is hardcoded `false` and documented as such — `context-gate` doesn't call `cache-fetch`/`cache-store` internally, and reporting fabricated `0`/`0` hit/miss counts would look measured when it isn't. Not tracked, disclosed rather than silently dropped: "repeated symbols" (would need `context-dependency-graph` data fed into the event log, not done) and "repeated context" (semantic manifest overlap, not just file-name repetition). Verified live: 3 real `context-gate` calls (2 with an identical task string) produced `calls: 3`, `repeatedPromptCount: 1`, and correct per-file frequency counts in `topRepeatedFiles`. Added Tier 10 to `stress.zig` (shape-only — exact counts are machine-state-dependent). `zig build test`: pass. `zig build stress`: 123/124 (1 pre-existing branch-name failure, unrelated) |
| 2026-07-02 | `symbol-index` implemented (M44, code index foundation); 2 pre-existing memory leaks found and fixed | Built entirely on existing primitives: `computeScan` for the file list (source-kind only), `computeOutline` for per-file extraction, and the existing `cache-fetch`/`cache-store` layer for persistence — no new storage mechanism. `indexOneFile` tries `computeCacheFetch(file, "symbol-index")` first; on a hit it deserializes via a new `parseSymbolsJson`; on a miss it calls `computeOutline` and `cache-store`s the result via a new `serializeSymbolsJson`. `Symbol.kind` must stay a static string literal (never freed) to match `computeOutline`'s existing convention, but JSON-parsed kind strings are heap strings — added `symbolKindStatic` to map a parsed kind string back onto one of the fixed literals rather than duping it, keeping every `Symbol.kind` uniformly non-owned regardless of source. **Found and fixed a real pre-existing leak in `computeCacheFetch`** (present since M20): it eagerly pre-built a `miss` result (`.path`/`.subKey` allocated) before checking the cache file, then on an actual hit built a *second*, separate result and returned that instead — orphaning the first allocation every single cache hit. Never surfaced before because nothing called `cache-fetch` in a tight per-file loop under the Debug build until `symbol-index` did. Fixed by resolving hit/miss first (in a labeled block), then allocating `.path`/`.subKey` exactly once at the end regardless of outcome. **Also found and fixed 2 instances of a different leak class in my own new code and one pre-existing sibling**: discarding `computeCacheStore`'s return value via `_ = ... catch {}` without freeing its owned `.path`/`.subKey` — fixed in the new `indexOneFile` and in the pre-existing `computeDeviceScan` write-through-cache call (same bug, same fix, found by grepping all `computeCacheStore(` call sites once the pattern was known). **Found but explicitly not fixed** (out of scope for this pass): `main.zig`'s `device-scan` dispatch leaks `result.arch`/`result.cpu`/`result.shell` etc. — unrelated to any code touched here, noticed only by chance while spot-checking `device-scan` for leftover leaks after the `computeCacheFetch` fix. Verified end-to-end: fresh cache → `cacheHits: 0, cacheMisses: N`; repeat call on the same project → `cacheHits: N, cacheMisses: 0`, identical `fileCount`/`symbolCount`, zero DebugAllocator leak reports on either call. Added Tier 11 to `stress.zig`, including a new `checkIntEq` helper (stress.zig had `checkIntGt` but no exact-match integer checker) to assert `cacheMisses == 0` on the warm-cache call. `zig build test`: pass. `zig build stress`: 130/131 (1 pre-existing branch-name failure, unrelated) |
| 2026-07-02 | `failure-record` / `failure-mark-fixed` / `failure-lookup` implemented (M45, failure-memory-v1, unreleased) | Requested scope explicitly excluded Recipe Registry, Capability Registry, AI Gate, and Zig Factory — kept to exactly: record a failed attempt, mark it later fixed, look up similar past failures, and count repeats. Sibling file to `ledger.json`/`solutions.json` (`~/.4orman/failures.json`, JSON-lines) rather than a new `LedgerEntry` field, same reasoning as M42: that struct backs the Claude-vs-Zig scoring protocol CLAUDE.md's guardrails depend on, and widening it was judged higher-risk than a sibling file reusing the existing matching algorithm. One file holds two record types distinguished by a `"type"` field (`"failure"` / `"resolution"`) rather than two files, because a resolution's only job is to reference a failure's `id` — keeping them together made `parseFailuresFile` a single read pass instead of two, and `failure-lookup` needs both loaded together anyway to correlate them. Marking a fix appends a new `"resolution"` line rather than rewriting the original `"failure"` line in place — the requested "keep schema simple, versioned, and append-only if possible" is satisfied literally, not just in spirit: no line in this file is ever rewritten after being written, mutation is expressed entirely as new appended facts. Deliberately no dedup-on-write (unlike `solutions-record`'s ≥50-score rejection) — a repeated failure is the exact signal `repeatedFailureCount` exists to surface, so rejecting the second occurrence would erase the data the feature was built to capture. `failure-lookup` reuses `ledgerWordOverlapScore` unmodified against `task + " " + errorSummary`, the same ≥50 threshold and the same function `findLedgerPrecedent`/`solutions-record` already use — "similar" is one definition across the codebase. `failure-mark-fixed` still appends a resolution even when `failureId` doesn't match any known failure (disclosed via `failureFound: false` in the response) rather than hard-failing — favors not losing data over rejecting an edge case like a cross-machine id not yet synced. Verified end-to-end live: recorded two near-duplicate failures, `failure-lookup` found both unresolved at score 100; `failure-mark-fixed` against the first; re-`failure-lookup` showed `resolved: true` with the fix text on that one only; `metrics.failureMemory` correctly reported `{"total": 2, "resolved": 1, "unresolved": 1, "repeatedFailureCount": 1}`. Test data cleaned up afterward (any pre-existing `~/.4orman/failures.json` was backed up before the smoke test and restored after — this machine had none). Added Tier 12 to `stress.zig`: same stdin limitation as Tiers 6/8 means `failure-record`'s and `failure-mark-fixed`'s happy paths are manually verified, not harness-covered; only usage/error paths and `failure-lookup` (no stdin) are asserted by the suite. `zig build -Doptimize=Debug`: pass. `zig build -Doptimize=ReleaseSafe`: pass. `zig build stress -Doptimize=Debug`: 135/136 (1 pre-existing branch-name failure, unrelated — confirmed by re-running the identical suite from `main`, where `git-cache branch=main` also fails unless the working tree is actually checked out to `main` at the moment the binary runs) |
| 2026-07-04 | `log-match` subcommand implemented (v0.69.0) | Domain-agnostic log-vs-pattern-library matcher — no domain vocabulary baked into the binary; pattern library is a caller-supplied JSON file (`{"patterns": [...]}`); `match: "glob"` supports `*` via `globContains` (fragment search within a line, not full-line anchored — different from the existing filename `globMatch` which IS anchored, kept as two separate functions since they solve different problems); reports `unmatchedSignalLines` (severity-marker lines with no pattern hit, capped 20) so genuinely novel errors surface instead of disappearing; real bug found writing the test: `matchCount` was computed AFTER `hits.toOwnedSlice(gpa)`, which clears the ArrayList — always capture counts before `toOwnedSlice` |
| 2026-07-04 | `ledger record-approach` + `tokens_spent` field added (v0.69.0) | Third ledger category, "approach" — solo-vs-multi-agent decisions, `winner` field reused for "solo"/"multi-agent" (same pattern as jungian reusing `winner` for the chosen path); `tokens_spent` is a new field on `LedgerEntry` (defaults to 0 via the same missing-field migration pattern as `category`/`shadow`/`outcome`), measured after the fact only — this subcommand never predicts token cost, since Zig has no way to know in advance what either approach will actually cost; excluded from `findLedgerPrecedent`/`findExactLedgerEntry` tool-choice lookups, same boundary as jungian (different kind of question, must never silently override a capability-check/route/build/test decision) |
| 2026-07-04 | `symbol-find` wired to consult the `symbol-index` cache for definition resolution (closes the M44 disclosed follow-up, unreleased) | `scanFileForSymbol` now calls `indexOneFile` (same cache-fetch-or-compute-and-store path `symbol-index` itself uses) before its own per-line scan; if the file's cached/parsed symbol list contains an exact name match, `def`/`def_kind`/its line come straight from that entry instead of being re-derived by `declarationKind()`'s independent heuristic. Deliberately does NOT touch reference-finding: `symbol-index` only records *declarations* (name/kind/line from `computeOutline`), never every textual occurrence, so there is no cached data a reference search could consult — the full per-line `wholeWordPos` scan still runs over every file exactly as before, source or not, and file coverage is unchanged (no narrowing to source-kind-only files, which would have silently dropped references living in docs/config/etc.). The one subtlety handled explicitly: when the cache resolves a definition line, that line is excluded from `refs` by comparing `line_num` against the cached line directly (`cached_def_line`), not by re-running `declarationKind()` on it — because `outlineExtract`'s heuristic (used by `computeOutline`/`symbol-index`) and `declarationKind()` (symbol-find's own, separate heuristic) are two different implementations that could in principle disagree on the same line, which would otherwise have let the definition line leak into `references` as a duplicate. `indexOneFile` never errors on unsupported/non-source file types — `computeOutline` just yields zero symbols for them — so calling it unconditionally on every file is safe and falls through to the pre-existing behavior unchanged whenever the cache has nothing to offer. Real, disclosed limit on the size of the win: since references still require reading and scanning every file's full content regardless, the benefit is skipping the redundant `declarationKind()` re-derivation and getting a single consistent source of truth for "what does this file declare" (shared with `outline`/`symbol-index`) — not an I/O reduction. Verified end-to-end live against this repo itself, both cold and warm cache, and on a repeat call for idempotency: `symbol-find <repo> indexOneFile` correctly resolved `definition` at `src/root.zig:5400` with `references` at `knowledge/decisions.md:88`, `src/root.zig:5475`, and the new call site `src/root.zig:7367` — with the definition line correctly absent from `references` in every run. `zig build -Doptimize=ReleaseSafe`: pass. `zig build test`: pass. No stress-suite coverage exists for `symbol-find` at all (pre-existing gap, not introduced here) — disclosed, not fixed in this pass. |
| 2026-07-05 | Fixed the `device-scan` leak disclosed but not fixed in M44 | `main.zig`'s `device-scan` dispatch printed `DeviceScanResult` and returned without ever freeing it — `.profile_id`, `.hardware.cpu`, `.hardware.arch`, each `.tools[].version`, the `.tools` slice itself, `.shell`, and `.path` are all owned (`[]u8`, heap-allocated in `computeDeviceScan`/`checkBinary`), none were freed. Confirmed live under the Debug build before fixing: 13 leaked allocations across 8 call sites (`root.zig:6630` `checkBinary`'s `.version` dupe, ×7 tools; plus `8153` arch, `8156` cpu, `8166` shell basename, `8183` the `tools_list` array itself, `8195` `profile_id`, `8279` `path`) on every single call. `hardware.os` (`"macos"`) and `optimal.zig_build_flags` (from `deviceScanZigFlags`) are excluded from the free list deliberately — both are static string literals, never allocated, matching the same non-owned-vs-owned distinction `Symbol.kind`/`symbolKindStatic` already established for M44. Fix mirrors the exact `defer { ... }` pattern `symbol-index`'s own `main.zig` dispatch already uses for its structurally similar nested-owned-data result, rather than inventing a new cleanup convention. Practical severity was low (device-scan is a short-lived CLI invocation — the OS reclaims everything on exit regardless), but left disclosed-and-unfixed since M44 was inconsistent with this repo's own standard of paying down found leaks immediately, as the sibling `computeCacheFetch` leak was in that same pass. Verified end-to-end live: repeated `device-scan` calls under the Debug build report zero leaks (down from 13 per call), output byte-identical to before the fix. `zig build -Doptimize=ReleaseSafe`: pass. `zig build -Doptimize=Debug`: pass. `zig build test`: pass |
| 2026-07-05 | `delegation-check` implemented (M48, unreleased) | Recommends solo vs. multi-agent for a task, ledger precedent first via a new `findApproachPrecedent` — deliberately not `findLedgerPrecedent`, which excludes `category == "approach"` entries on purpose (values/tool-choice boundary, M47) — so this needed its own sibling lookup scoped to exactly what that one skips, same `ledgerWordOverlapScore` scoring, ≥50 threshold, non-stale only. On a ledger miss, falls back to a heuristic over `complexity-tier` (`trivial|standard|full-build`, the exact taxonomy already in this workspace's own CLAUDE.md — deliberately not a second classification scheme): `trivial` → solo, `full-build` → multi-agent (`suggestedCount: 2`), `standard` with no precedent → solo as the tie-break default with `needsDecision: true`, mirroring `build`/`run-tests`'s existing `resolvedBy: "tie-break"` contract for ambiguous cases exactly rather than inventing new vocabulary for the same concept. Deliberately never predicts token cost — `record-approach` (M47) already established Zig has no way to know either approach's actual cost in advance; this subcommand pattern-matches recorded outcomes and a fixed taxonomy only, never a forecast. Real bug caught before landing, not after: `.recommend`/`.reason`/`.ledgerId` were originally static string literals in the three heuristic branches but heap-duped in the ledger-hit branch, while `main.zig`'s dispatch frees all three unconditionally — a mix of owned/static across branches would have corrupted the allocator on every heuristic-path call. Fixed by heap-duping unconditionally on every return path, including the branches that look like static literals. While fixing it, found a structurally identical pre-existing leak in `capability-check`'s own `main.zig` dispatch (`result.description`/`result.args`/`result.ledger_id`/`result.ledger_reasoning` are never freed at all) — disclosed, not fixed here, out of scope for this pass. Verified end-to-end live across all four paths (ledger-hit, trivial, full-build, standard-with-no-precedent) plus the invalid-tier usage-error path, using a real recorded `ledger record-approach` entry from the actual `symbol-find` task earlier this session, not synthetic test data — zero `DebugAllocator` leak reports on any path. `zig build -Doptimize=ReleaseSafe`: pass. `zig build -Doptimize=Debug`: pass. `zig build test`: pass. No stress-suite coverage added, same disclosed gap as `symbol-find` |
| 2026-07-05 | Fixed a leak in `public-release-check` (found while actually using it for the first time, for the delegation-check release) | `main.zig`'s `public-release-check` dispatch never freed `result.findings` at all — same bug class as the M44 `device-scan` leak fixed earlier the same day: every `PrcFinding.detail` is always heap-allocated, and `.file` is heap-allocated whenever a specific file is named (empty `""` literal only for repo-wide findings like `history-emails`/no-watchlist `company-names`). Confirmed live under the Debug build before fixing: 62 leaked allocations across `prcScanFileForTerms`, `prcCheckHistoryEmails`, and `computePublicReleaseCheck` on a single call against the `4orman-tools` repo itself. This is the subcommand's *second* disclosed bug — CHANGELOG (v0.70.0) already records a use-after-free fix for it, and it has no `spec.md` milestone entry at all (added directly via CHANGELOG, a pre-existing gap not addressed here). Fix mirrors the exact ownership rule the function's own `errdefer` block already establishes (`if (f.file.len > 0) gpa.free(f.file)`, `gpa.free(f.detail)` unconditionally) — the success path just never applied it. Verified end-to-end live: repeated `public-release-check` calls under the Debug build report zero leaks (down from 62/call), `ready`/`watchlistConfigured`/`findings` output unchanged. `zig build -Doptimize=ReleaseSafe`: pass. `zig build -Doptimize=Debug`: pass. `zig build test`: pass |
| 2026-07-05 | `curriculum-check` / `curriculum-list` / `ledger record-curriculum` implemented (M49, unreleased) | ROI-ranked learn/build inventory per tech domain — same "check the ledger's memory before reasoning fresh" discipline as `capability-check`/`route`/`delegation-check`, applied to a new domain of question (what's worth learning/building for a profession, not which tool to use). New `"curriculum"` ledger category, four new `LedgerEntry` fields (`domain`, `artifact_type`, `artifact_ref`, `status`), reusing `question`/`winner`/`shadow`/`synthesis` exactly as jungian/approach already established the one-free-text-field-per-category pattern — no new columns for concepts that already have a home. `status` is derived from `artifact_ref.len > 0` rather than a separate argument: Zig only ever stores what Claude decided (confirmed design call), it never scaffolds the artifact, so there's no separate "mark built" event to track beyond what `artifact_ref` being non-empty already implies at record time. Deliberately **no heuristic fallback** on a ledger miss, unlike `delegation-check` — that subcommand has a fixed trivial/standard/full-build taxonomy to fall back on; arbitrary tech topics have no equivalent, so a miss is always `needsDecision: true` with a reasoning string explaining why, never a guessed ranking. New `findCurriculumPrecedent` matches a `"<domain> <topic>"` combined query against a stored entry's own `"<domain> <question>"`, so same-worded topics in different domains don't cross-match — deliberately not a two-field matcher, the caller builds the combined string once. `findLedgerPrecedent`/`findExactLedgerEntry` both updated to also exclude `category == "curriculum"`, same boundary as jungian/approach (never silently override a tool-choice decision). **Two real bugs caught and fixed before landing, via the Zig compiler itself, not by inspection:** widening `LedgerEntry` by four fields turned every pre-existing construction site missing them into a compile error — `zig build`'s "missing struct field" diagnostics named `findLedgerPrecedent`, `findApproachPrecedent`, and `findExactLedgerEntry` precisely, each fixed in turn, confirming no site was silently skipped. Separately, `curriculum-check`'s JSON output initially printed `artifactRef` unescaped — it's caller-supplied free text (a path), not a closed-vocabulary value like `roiTier`/`artifactType`, and needed the same `allocJsonEscape` treatment as `domain`/`topic`/`reason` get; caught on review before the live verification pass, not after. **Disclosed, not fixed — pre-existing, not introduced by this milestone:** `main.zig`'s general `ledger` dispatch (backing `show` and every `record*` mode, including this one) never frees `lresult.entries` or any entry's owned fields at all, for any category — confirmed live under the Debug build, 32 leaked allocations calling `ledger show`/`ledger record-curriculum` with 2 entries present. Same root cause, same day, as the `capability-check` leak disclosed during M48 — a dispatch that never frees what `compute*` handed back. This milestone's four new fields make each leaked entry's footprint marginally larger but did not create the leak; fixing the shared `ledger` dispatch itself was out of scope for this pass. Verified end-to-end live: recorded a real entry (`full-stack-dev` / "React state management", `highest`/`doc`, no artifact yet → `status: "proposed"`), confirmed `curriculum-check` finds it (`source: "ledger"`) and `curriculum-list` shows it; confirmed a pre-existing `"approach"`-category entry from earlier the same day still parses correctly with all four new fields defaulting to `""` — the migration doesn't break old entries. Zero `DebugAllocator` leak reports on every new subcommand's own code path (`curriculum-check` hit/miss, `curriculum-list` populated/empty); only the pre-existing, disclosed `ledger` dispatch leak shows up, and only on the two calls that route through it. `zig build -Doptimize=ReleaseSafe`: pass. `zig build -Doptimize=Debug`: pass. `zig build test`: pass. No stress-suite coverage added, same disclosed gap as `symbol-find`/`delegation-check` |
| 2026-07-05 | `architecture-check` / `architecture-list` / `ledger record-architecture` implemented (M50, unreleased) | Narrower, stricter sibling to M49's curriculum ledger — scoped specifically to proposed changes to 4orman's own architecture (a new guardrail, subcommand, or hook), not arbitrary external tech domains. The key difference: curriculum has no gate at all (every proposal is a values call, `needsDecision: true` on every miss); this one has a hard numeric bar — `estimated_improvement_pct >= 1.0 and !has_drawbacks` — below which `architecture-check` always returns `recommend: "reconsider-with-user"`, never a silent build and never a silent drop. Two new `LedgerEntry` fields (`estimated_improvement_pct: f64`, `has_drawbacks: bool`), defaulting to `0.0`/`false` for every other category via the same migration pattern M49 established for `domain`/`artifact_type`/`artifact_ref`/`status` — but being primitive types rather than `[]u8`, neither needs a free call anywhere in the codebase, so unlike M49's four fields these add literally zero new leaked allocations to the pre-existing disclosed `ledger`-dispatch leak (still present, still not this milestone's to fix — see below). `status` (`proposed\|built\|rejected`) is a **required explicit argument** to `record-architecture`, deliberately not derived the way curriculum's is from `artifact_ref` — "rejected" is a real, expected outcome here (a proposal considered and turned down) with no artifact-path signal that could ever imply it. On a ledger hit, `recommend` checks the stored `status` before the gate at all: `"built"`/`"rejected"` short-circuit to `"already-built"`/`"already-rejected"` regardless of the stored percentage, and only a `"proposed"` entry falls through to the actual gate check against its stored numbers. Reused M49's `curriculumTierRank` for `architecture-list`'s sort rather than writing a second identical highest/medium/lowest ranking function — same three-value vocabulary, no reason to duplicate it. `metrics` gained a new `architectureLedger` object (`total`, `byTier`, `byStatus`) via `computeArchitectureLedgerStats`, directly answering the "monitor growth over time" ask: call `metrics` repeatedly and watch the counts move, same instrumentation pattern `contextGate`/`failureMemory` already established rather than inventing a new one. Verified the field-ownership discipline explicitly before calling this done, not just via the compiler: every string field on all three of `computeArchitectureCheck`'s return paths (ledger-hit, gate-pass-miss, gate-fail-miss) is heap-duped unconditionally — including the branches returning `"build"`/`"reconsider-with-user"`/`""`, which look like they could safely be static literals but cannot be, since `main.zig`'s dispatch frees all of them unconditionally. This is the exact bug M48's `delegation-check` shipped with a fix for after almost landing it broken; deliberately not repeated here. `.source` (`"ledger"`/`"gate"`) is the one field left as a static literal on every path, correctly never freed in `main.zig`, matching the identical precedent `delegation-check`/`curriculum-check` already set for their own `.source` fields. **Disclosed, not fixed — the same pre-existing leak M49 already disclosed, not new or worsened:** `main.zig`'s general `ledger` dispatch still never frees `lresult.entries` or any entry's owned fields, for any category. Confirmed live under the Debug build: 42 leaked allocations with 4 entries now present (up from 32 with 2 at M49's disclosure) — proportional to entry count and owned-string-field count, the same rate as before, not an increase caused by this milestone's 2 new fields (which, being non-`[]u8`, contribute zero additional leaked allocations per entry). Verified end-to-end live against the real `~/.4orman/ledger.json`, not a fixture: recorded a genuinely plausible entry (caching the binary-version check across a session instead of re-running `compat-check` every session start — `highest`/2.5%/no drawbacks/`proposed`), confirmed the ledger-hit path (`source: "ledger"`, `recommend: "build"`), confirmed all three gate-check branches fresh (gate-pass, has-drawbacks, below-threshold), confirmed `architecture-list` and `metrics.architectureLedger` both count the new entry correctly, confirmed the pre-existing `"approach"`/`"curriculum"` entries from earlier the same day still parse correctly with both new fields defaulting to `0.0`/`false`. Zero `DebugAllocator` leak reports on any new subcommand's own code path. `zig build -Doptimize=ReleaseSafe`: pass. `zig build -Doptimize=Debug`: pass. `zig build test`: pass. No stress-suite coverage added, same disclosed gap as `symbol-find`/`delegation-check`/`curriculum-check` |
| 2026-07-05 | Fixed both disclosed-not-fixed leaks: `capability-check` (M48) and the general `ledger` dispatch (M49) | Neither was a single simple oversight — both turned out to be two separate bugs each once actually traced. `capability-check`: (1) `computeCapabilityCheck`'s own ledger-hit `defer` freed only 3 of `findLedgerPrecedent`'s 8 duped fields, leaking `category`/`shadow`/`synthesis`/`outcome`/`outcome_matched` internally regardless of what any caller does — a bug inside the compute function itself, not just an uncalled-free in `main.zig`. (2) `main.zig`'s dispatch separately never freed the two fields that DO transfer ownership (`ledger_id`/`ledger_reasoning`), while correctly needing zero frees for `subcommand`/`description`/`args` (always static — the registry's comptime strings or `""` literals, never duped on any path). Fixed both, gating the `main.zig` free on `source == "ledger"` so the always-static fields are never touched. Verified live for the two branches that have real, reachable data (`native`, `claude`-fallback) — the `ledger` branch has no `"rps"`-category entry in this machine's actual ledger to trigger it (`findLedgerPrecedent` deliberately excludes every other category), so that path was verified by code trace, not live execution, disclosed rather than fabricating a fake entry just to exercise it. The general `ledger` dispatch leak was worse than its own disclosure implied: `main.zig`'s print loop for `show`/every `record*` never freed any of `LedgerEntry`'s 14 owned string fields or the `entries` slice — that part was exactly as disclosed, fixed with a straightforward `defer`. But verifying that fix live (checking `ledger check-stale`, not just `show`) surfaced a second, independent bug nobody had disclosed at all: `check_stale` mode builds a separate `stale_list` by shallow-copying matching entries (copying `[]u8` pointers, not bytes) and never freed the original `entries` list — with 0 stale entries in the real ledger, all 4 real entries' full data leaked with an empty result to show for it. The fix required care specifically because of the shallow copy: free each *non-stale* entry's fields explicitly (orphaned once `entries` is discarded, since `stale_list` never referenced them), then release `entries`' own backing array via `.deinit(gpa)` rather than `toOwnedSlice` — the stale entries' strings already transferred ownership to `stale_list`/`result_entries`, and freeing them here too would double-free once the caller's own `defer` runs. Verified live: `ledger show` 42→0 leaked allocations, `ledger check-stale` 42→0, both byte-identical output before and after. `validate` and every `record*` mode share the same `else` branch already verified via `show` — the code structure guarantees identical behavior, not separately re-tested. `zig build -Doptimize=ReleaseSafe`/`-Doptimize=Debug`/`test`: all pass |
| 2026-07-05 | Fixed `public-release-check` reporting a false `ready: true` on non-git scan targets (M52) | `prcTrackedFiles` swallowed *any* `git ls-files` failure — including "not a git repository" — as an empty file list, indistinguishable from a repo with genuinely zero tracked files. Every downstream check then scanned nothing and reported clean. Discovered live: refreshing `4orman-public` reported `ready: true` with zero findings on a scratch copy checked before `git init`, purely because of this bug, not because it was actually clean (manual grep caught the real leaks that were still present). Reproduced deterministically with a bare non-git directory containing an obvious email + home-path leak. Fixed by propagating the git error instead of swallowing it, and appending a high-severity `structure` finding when it fires — forces `ready: false` using the existing JSON shape. Added a regression test; `std.testing.tmpDir` turned out to nest inside this repo's own `.zig-cache/`, so `git -C` on it walks up and finds this repo's real `.git` instead of failing as intended — worked around with a hardcoded nonexistent path instead, a reminder that this test harness's tmp dirs are not reliably outside every git tree. Verified live against both the repro case (now `ready: false`) and the real repo (unaffected, `ready: true` genuinely). `zig build test`: 73/73 pass, including the new regression test. |
| 2026-07-05 | Session-lifetime cache for `compat-check`'s per-tool version lookups (M53) — the `architecture` ledger's own oldest `proposed` entry, built | Cached the expensive part (a subprocess spawn per tool) to `~/.4orman/compat-check-cache.json`, keyed by a 5-minute TTL rather than a file hash — there's no single file whose content maps to "current installed tool versions," so the existing hash-based `cache-fetch`/`cache-store` convention didn't fit; this needed its own time-based scheme. Chose a short TTL deliberately, not a long one: caching for e.g. a full hour would eliminate more subprocess spawns but widens the window where a tool upgraded in one terminal still reads as its old version from a fresh terminal within the cache's lifetime — undermining the exact moment compat-check exists to catch drift. Recorded the TTL choice itself via `ledger record-jungian` rather than presenting it as a measured optimum, since there's no provably-correct number, only a tradeoff between spawn-savings and staleness-masking risk. Verified live: cache-miss call measured 0.319s wall-clock vs. 0.050s on the immediate cache-hit re-run (~6x, a single measured sample, not a general guarantee); drift is still correctly detected when fed from cached data (deliberately corrupted the baseline's `zig` entry, confirmed `ok: false` with correct `was`/`now`/`rollback`); the expired-cache path correctly falls back to a fresh full check (backdated `cached_at_epoch`, confirmed same ~0.316s cost as a genuine miss). Zero leaks on all three paths. Recorded `ledger record-architecture` with `status: built` on the same topic/id as the original `proposed` entry — the ledger is append-only, so both entries persist; the newer one is what any future lookup should treat as current. `zig build -Doptimize=ReleaseSafe`/`-Doptimize=Debug`/`test`: all pass |
| 2026-07-05 | Fixed `release-info`'s false-clean `isDirty` on a failed `git status` (M54) | Discovered by cloning `foreman-codex-tools` — a previously-unknown, dormant early ancestor of this repo (last touched 2026-06-30, before the 4ORMan rebrand) — and finding the identical bug there first while auditing it per standing instruction to check every project for a bug class once it's found anywhere. `computeReleaseInfo`'s status check swallowed any `git status --porcelain` failure as `null`, then read that the same as "nothing changed" (`isDirty: false`) — the same false-clean shape M52 already fixed in `public-release-check`, except here it feeds `/release`/`/brew-release`'s own pre-flight dirty gate, so a swallowed failure could let a release ship over uncommitted work. Fixed by propagating the failure via `try`, mirroring this same function's pre-existing `rev-parse HEAD` error handling — required no `main.zig` dispatch change since a generic `else` branch already existed there. Verified live: clean repo `false`, a genuinely dirty repo `true`, non-git path unaffected (caught earlier by the `rev-parse HEAD` check as before). Zero leaks. `zig build -Doptimize=ReleaseSafe`/`-Doptimize=Debug`/`test` (73/73): all pass. Applied the identical fix to `foreman-codex-tools` in the same pass, per the cross-project rule this bug was found under. |
| 2026-07-05 | `ledger record-bug` / `ledger find-bug` implemented (v0.73.2) — a shared bug-fix ticket ledger across `4orman`/`00orman`/`02orman` | User asked for something like a "ticket support ledger" for errors and their fixes; scoped down to two explicit choices (one shared global ledger, not per-project; minimal fields — signature/fix/status, not a full lifecycle). `architecture-check` returned `reconsider-with-user` twice (4% both times, real drawbacks: new subcommand surface, unproven match quality) — user explicitly directed the build anyway via plan mode, same override pattern as M-ingestion-queue in `02orman`. New `"bug"` `LedgerEntry` category reuses every existing field rather than a new file/struct: `domain` = project, `question` = error signature (the match key), `reasoning` = fix, `status` = `"fixed"`/`"open"`. `record-bug` slots into `computeLedger`'s existing switch exactly like `record-architecture`. `find-bug` is a standalone function (`computeFindBug`, sibling to `findLedgerPrecedent`, not part of `LedgerMode`/`computeLedger` — its return shape (scored matches) doesn't fit the generic `LedgerResult`), filtering to `category == "bug"` and scoring via the *existing* `ledgerWordOverlapScore` helper — deliberately not porting `02orman`'s Jaccard matcher in, since this codebase already had an equivalent scorer. Default threshold 30 (lower than `findLedgerPrecedent`'s 50) since bug reports of the same issue are often worded quite differently than a repeated architecture question; returns every match above threshold with a `score` field so Claude judges confidence rather than trusting a single hit. Verified end-to-end live, not synthetic: recorded the real `std.json.parseFromSlice` use-after-free bug fixed earlier this session (id `48b26df4a8434339`), then `find-bug` matched it (score 44) against deliberately different wording ("json parseFromSlice corrupted strings after freeing the read buffer"), and a genuinely unrelated query ("docker container fails to start on port conflict") correctly returned zero matches. Zero `DebugAllocator` leak reports on both new code paths. `zig build -Doptimize=ReleaseSafe`/`-Doptimize=Debug`/`test` (73/73): all pass. Recorded the architecture decision itself via `ledger record-architecture` (id `087c6eaa7481f692`). |
| 2026-07-05 | `ledger record-feature` implemented + `record-bug` gains vendor-email drafting (v0.73.3) | Two asks in one pass: (1) a feature-support-ticket ledger for evaluating pasted brainstorms/ideas the way `/new-project`'s spec interview would, biasing toward the richest feasible scope rather than a minimal one; (2) when a recorded bug involves a 3rd-party vendor, draft a ready-to-review email rather than just filing the ticket. `architecture-check` returned `reconsider-with-user` again (3%, real drawbacks) — user explicitly directed via plan mode, same pattern as the two prior builds this session. New `"feature"` category mirrors `"curriculum"`'s exact shape (`domain`/`topic`-as-`question`/tiered-`winner`/`shadow`/`synthesis`/derived-`status`) field-for-field — zero new `LedgerEntry` fields. Vendor-email drafting repurposes the already-bug-unused `artifact_type` field to store the vendor name (same one-field-multiple-meanings convention `winner`/`shadow`/`synthesis` already established per-category) — also zero new entry fields, just one new `VendorEmailDraft` struct and one new `LedgerResult.vendor_email` field. `computeLedger`'s `.record_bug` case, after appending the new entry, scans all `"bug"` entries whose `artifact_type` case-insensitively matches the given vendor (deliberately including *past* tickets, not just the one just filed) and composes one bullet per match (`question`, plus a workaround note when `reasoning` is non-empty) and a subject line that branches on match count. `signature` is a literal placeholder (`"Best regards,\n[Your Name]"`) — never a guessed real name. Nothing here sends email; Zig has no transport, and `needsFrom`/`needsTo`/`needsCc` (computed in `main.zig`'s print step, not stored) are purely the signal for Claude to ask the user for those addresses in conversation. Verified end-to-end live: recorded two real bugs against the same fictitious vendor ("Acme Sync") and confirmed the second call's draft aggregates both issues, not just the latest; confirmed a vendor-less `record-bug` call returns `vendorEmail: null`; recorded a real feature-support-ticket evaluating this session's own "pit-stop/airplane" brainstorm (`needs-input` readiness — no concrete product requirement exists yet, so `/new-project`'s spec interview would stall on "who is the user"). Zero `DebugAllocator` leak reports on every new/modified path (`record-bug` with and without vendor, `record-feature`, `find-bug` unaffected). `zig build -Doptimize=ReleaseSafe`/`-Doptimize=Debug`/`test` (73/73): all pass. Recorded the architecture decision itself via `ledger record-architecture` (id `b2cfc9e11018668d`). |
| 2026-07-05 | `deep-scan` implemented + `Stop`/`SessionStart` hook wiring (v0.73.4) | User asked for a "rabbit hole style" scan of every file in a project, once per session, feeding the `record-feature`/`record-bug` ledgers just built. `architecture-check` returned `reconsider-with-user` (8%, real drawbacks) -- user explicitly directed via plan mode. `computeDeepScan` composes three already-built capabilities, no new scanning algorithm: `computeContextScan`'s `topFiles` (large files), `computeSecretScan`'s real findings, and two `computeGrep` sweeps (`TODO`/`FIXME`) -- deduped by path, each candidate gets a `computeOutline` summary (function/class map, not full content) so Claude has real structural context before reading anything. Session-to-session dedup via `~/.4orman/deep-scan-seen.json` (path+project key -> `sha256Hex` content hash, the same hash function ledger IDs already use) -- without it, an unresolved TODO would re-surface and get re-reviewed every single session forever; a file that changes (or is flagged for the first time) always reappears. Extracted the `Stop` hook from an inline `.claude/settings.json` one-liner into `bin/stop-hook.py` (matching `SessionStart`'s existing script pattern) -- same cache-purge/promotion-queue behavior, plus three `deep-scan` calls (`4orman`/`00orman`/`02orman`) aggregated into `~/.4orman/deep-scan-pending.json`. `session-start-hook.py` surfaces `deepScanPending`. **Two real bugs caught and fixed during verification, not by inspection:** (1) every source path from `context-scan`/`secret-scan`/`grep` is relative to the scanned root, not absolute -- `computeDeepScan` initially called `openFileAbsolute` on them directly, hitting a real safety-assert panic (`path.isAbsolute` failing) on the very first live run against `4orman` itself; fixed by joining `project_path` + the relative path before any file I/O. (2) `readSeenFile`'s empty-fallback value was constructed unconditionally at the top of the function (even on the success path, where it's simply discarded) -- leaked one arena per call, caught live via a Debug-build run against `00orman`/`02orman` (`error(DebugAllocator): memory address ... leaked`, traced to `gpa.create(std.heap.ArenaAllocator)` at the top of `readSeenFile`); fixed by only constructing it lazily inside each `catch` branch (`emptySeenFile` helper). Verified end-to-end live against all three real projects: first scan of `4orman` found 14 real candidates (large files up to 647KB); a second immediate scan correctly found zero (dedup working); `00orman`/`02orman` scanned clean (zero leaks) after the fix; the full `Stop`-hook-to-`SessionStart`-hook pipeline confirmed working with the local build temporarily prepended to `PATH` (the installed Homebrew binary is stale, same gap as the antigravity-queue feature -- needs `/brew-release` before this reaches a real session's `PATH`). Zero `DebugAllocator` leak reports after both fixes. `zig build -Doptimize=ReleaseSafe`/`-Doptimize=Debug`/`test` (73/73): all pass. |
| 2026-07-06 | `ledger check-precedent` implemented + two `computeRegistry()` self-description gaps fixed (M55, unreleased) | Requested after a live demo of the new sandbox-benchmark-then-ledger workflow surfaced that "check for a precedent" meant reading all of `ledger show` by hand -- `findLedgerPrecedent`'s fuzzy match was real but only reachable indirectly, embedded inside `build`/`run-tests`/`capability-check`'s own tool-choice resolution, never exposed as a direct query. `computeLedgerCheckPrecedent` is a thin wrapper: same function, same 50%-word-overlap threshold, same jungian/approach/curriculum/architecture exclusion, same staleness filter -- zero new matching logic. Formats and returns a JSON string directly, so nothing survives past the function; all 14 of `LedgerEntry`'s owned string fields are freed unconditionally, sidestepping the partial-free bookkeeping `capability-check` needed after its M51 leak fix (there, `.id`/`.reasoning` transfer to the return struct and the other 8 fields get freed in `main.zig`'s defer; here, all 14 can just go). While verifying this live, found `deep-scan` entirely missing from `computeRegistry()` (worked correctly when invoked directly, the registry just never listed it) and `ledger`'s own registry `args` string missing `record-bug`/`find-bug`/`record-feature` (present in `main.zig`'s usage-help text at a different call site, never carried over to the registry). Filed via `ledger record-bug` first, then fixed in the same pass -- the bug-ticket mechanism being verified was used to file the bug found while verifying it. Verified live: `check-precedent` against a real recorded benchmark entry (from the sandbox test that motivated this) matched via a differently-worded query at 75% overlap, well above the 50% threshold; not-found path returns all-null fields; missing-arg usage error exits 1. Zero `DebugAllocator` leak reports on both found and not-found paths. `zig build -Doptimize=ReleaseSafe`, `-Doptimize=Debug`, `zig build test` (73/73): all pass. No unit test for the ledger-lookup path itself -- same disclosed `$HOME`-dependent gap as `delegation-check`/`curriculum-check`/`architecture-check`. |
| 2026-07-06 | `session-heartbeat`/`session-unregister` + `SessionEnd` hook implemented (M56, unreleased) | User asked to revisit the multi-agent council/Historian backlog item; on inspection it doesn't address the actual concrete forcing need from the same day (two independent sessions built the identical hook and the identical subcommand with zero coordination) -- the council proposal is about structuring one session's own decision process, not detecting concurrent sessions -- so this is a new, narrowly-scoped mechanism instead of resurrecting that proposal | Verified Claude Code's own hooks documentation and issue tracker *before* designing anything, not assumed: every hook event's stdin includes `session_id`, confirmed stable across `SessionStart`/`SessionEnd` for the same session -- but `SessionEnd` can be bypassed entirely by `SIGKILL` or closing the terminal, so it can never be the sole cleanup signal; `Stop` firing reliably every turn is the real refresh mechanism a TTL can trust. `active-sessions.json` follows the exact `deep-scan-seen.json` convention already established (arena-backed `std.json.Parsed` empty fallback, manual JSON-string build, atomic tmp-then-rename write) -- no new storage pattern. `computeSessionHeartbeat` upserts the caller's `(session_id, repo)` entry, prunes anything older than `SESSION_TTL_SECS` (900s = 15 min) on every call regardless of explicit unregistration, and returns every other non-stale entry on the same repo; `computeSessionUnregister` is a plain best-effort removal, wired into a new `bin/session-end-hook.py` and a new `SessionEnd` block in `.claude/settings.json`. `bin/session-start-hook.py` and `bin/stop-hook.py` both gained a `read_stdin_session_id()` helper (previously neither script read stdin at all) and now call `session-heartbeat` -- SessionStart surfaces `otherActiveSessions` once in `additionalContext`/`systemMessage`; Stop re-surfaces it every turn the collision persists, deliberately not just once, since a persistent coordination problem shouldn't go quiet after the first mention. Caught live during testing, not by inspection: the first manual test of the wired-up hooks silently reported no collision even with two real registered sessions, traced to the hook scripts calling bare `4orman-tools` on `PATH`, which was still the unreleased-feature-lacking `v0.73.5` binary -- same "verify the real artifact" lesson from earlier the same day, this time caught before shipping rather than after. Verified live end-to-end with the local build temporarily on `PATH`: two simulated sessions on the same repo detect each other in both directions; a session on a *different* repo sees nobody (no cross-project false positives); a manually-injected 60-minute-stale entry is pruned and never surfaces; `session-unregister` actually empties the file, not just that call's output; the full `SessionStart`→`Stop`→`SessionEnd` chain confirmed working together, not just each hook in isolation. Zero `DebugAllocator` leak reports on register, other-active, and unregister paths. `zig build -Doptimize=ReleaseSafe`, `-Doptimize=Debug`, `zig build test`: all pass. No unit test for either subcommand -- same disclosed `$HOME`-dependent gap as every other `findLedgerPrecedent`-adjacent addition this session. |
| 2026-07-06 | `ledger review-due` + `names-check` implemented (M57, unreleased) | User asked to offload more to Zig; identified two concrete, already-repeated-2+-times cases from the same session rather than guessing at candidates -- `outcomeReviewDue` filtering reimplemented 3 times, doc-vs-reality auditing done twice by hand | `review-due` is a `LedgerMode` variant mirroring `check_stale` exactly (same shallow-copy/selective-free shape, same `LedgerResult`, same generic `main.zig` print path already used by every other mode) -- filters on `outcome_review_due` instead of `is_stale`, nothing else changes. `names-check <doc-path> <comma-separated-names>` deliberately does not hardcode either concrete case (commands-vs-help.md, registry-vs-CLAUDE.md) that motivated it -- caller supplies the names list, subcommand only does whole-word presence checking, reusing `containsWholeWord` (pre-existing, previously only used inside ledger tool-choice resolution) unmodified. Self-verifying on its first real run: checking `CLAUDE.md` against the actual registry immediately found `session-unregister` and `names-check` itself missing from the table -- fixed in the same pass. Verified live: `review-due` returns empty against the real ledger (matches the already-known `outcomeReviewDue: 0`), correctly finds a synthetic 40-day-old `outcome_matched: "unknown"` entry when injected, correctly excludes it again once removed -- real ledger backed up before the test, restored after, never left polluted. `names-check` verified against both motivating cases (0 missing on `help.md`; 2 missing on `CLAUDE.md`, 0 after the fix) and both error paths (nonexistent doc, missing args). Zero `DebugAllocator` leak reports on `review-due`'s empty and non-empty paths and both `names-check` paths. `zig build -Doptimize=ReleaseSafe`, `-Doptimize=Debug`, `zig build test`: all pass. No unit test for `review-due` (same disclosed `$HOME` gap as every other ledger-reading addition); `names-check` has no such dependency and also has none, consistent with `computeFindFiles`/`computeGrep` (its closest structural siblings) also lacking dedicated unit tests in this codebase. |
| 2026-07-06 | Fixed `log-match` allocator corruption: `main.zig`'s handler passed `args[2]` straight into `computeLogMatch` -> `openFileAbsolute` without resolving it first, the one path-taking subcommand in the whole file skipping the `root.resolveAbsolutePath` step every other one already does | Discovered while building a downstream caller's traffic-pattern set (unreleased) on top of the existing log-match integration -- a relative pattern-file path (`knowledge/jenkins-patterns.json`, exactly how that caller's script invokes it) crashed with `panic: reached unreachable code` inside `computeLogMatch`, at a line with no `unreachable` in it. This repo's own CLAUDE.md already documents the exact failure mode ("relative paths into `*Absolute` APIs are undefined behavior in this Zig version -- confirmed as allocator corruption, not a clean error", 2026-07-02) -- the crash site was noise from corrupted heap state, not the real bug | Added the same `root.resolveAbsolutePath(gpa, io, args[2]) catch { ... }` / `defer gpa.free(abs_path)` pair every other path-taking subcommand already has, then passed `abs_path` (not `args[2]`) into `computeLogMatch`. No signature or schema change -- `computeLogMatch` itself was never wrong, its caller just never gave it what its precondition required. Verified live: the exact relative-path invocation that used to panic now returns a clean `{"matchCount":0,...}` against unrelated pattern content and real, non-zero matches against on-topic content; an absolute-path invocation against a 200-line real log bundle (nginx access/error, php-fpm, php-slow) returned 78 matches with no crash. `zig fmt --check` on the changed hunk only (repo has pre-existing unformatted code elsewhere, not touched). `zig build -Doptimize=ReleaseSafe` and `zig build test`: all pass, exit 0. This also means the Homebrew-installed binary (`v0.73.7`, same version as this fix's base) has been silently crashing on every `log-match` call made with a relative path since it shipped -- that downstream project's ticket-enrichment feature has likely been failing (degrading gracefully to an unenriched ticket, per its own design) in every real session until this is released. Needs `/brew-release` before it reaches the installed binary; not yet committed or released as of this entry. |
